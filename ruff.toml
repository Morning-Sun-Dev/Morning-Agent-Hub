[lint]
# E402: logging 설정 후 import하는 패턴 허용 (에이전트 서버 공통 패턴)
# F401: 미사용 import — 추후 정리
# F541: f-string placeholder 없음 — 로그 메시지에서 빈번
ignore = ["E402", "F401", "F541"]

exclude = [
    "__pycache__",
    ".git",
    ".venv",
    "node_modules",
    "frontend",
]
